import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 15,
  },
  location: {
    paddingVertical: 30,
    textAlign: "center",
  },
  sharePictureButtonContainer: {
    alignItems: "center",
  },
});
