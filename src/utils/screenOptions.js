const main = () => ({
  headerTitle: "PicTrackr",
  headerTitleAlign: "center",
});

const picture = () => ({
  headerTitle: "",
});

const takePicture = () => ({
  headerTitle: "",
});

export const options = {
  main,
  picture,
  takePicture,
};
